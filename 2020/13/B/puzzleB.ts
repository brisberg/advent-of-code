/**
 * Specialized PuzzleFunction for Puzzle 13.B.
 *
 *
 */
export function puzzleB(inputs: string[]): number {
  if (inputs.length !== 2) {
    throw new Error(
        `Inputs malformed. Expecting two lines denoting 'start time' and active bus lines.`)
  }

  return 0;
}
